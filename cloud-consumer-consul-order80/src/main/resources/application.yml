server:
  port: 80

spring:
  application:
    name: cloud-order-service
  cloud:
    consul:
      # 主机
      host: localhost
      # 端口
      port: 8500
      discovery:
        # 服务名
        service-name: ${spring.application.name}